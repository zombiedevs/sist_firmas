<%= form_for(@principal) do |f| %>
  <% if @principal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@principal.errors.count, "error") %> prohibited this principal from being saved:</h2>

      <ul>
      <% @principal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :request_type_id %><br />
    <%= f.number_field :request_type_id %>
  </div>
  <div class="field">
    <%= f.label :fund_id %><br />
    <%= f.number_field :fund_id %>
  </div>
  <div class="field">
    <%= f.label :lista %><br />
    <%= f.number_field :lista %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
